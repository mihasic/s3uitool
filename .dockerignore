.git
.gitignore
.dockerignore
.github
.vscode
README.md
docker-compose.yml

# Build artifacts — use **/ so nested copies (app/node_modules, api/dist, …) are
# excluded too, not just the context root.
**/node_modules
**/dist
**/build
